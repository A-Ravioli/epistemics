/** Bundled starter packs and versioned prompt files. Packs are plain JSON Curriculum objects (see @epistemics/core CurriculumSchema). */
export const PACK_FILES = ['probability-basics.epistemics.json'] as const;
